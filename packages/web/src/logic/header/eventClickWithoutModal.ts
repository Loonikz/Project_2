// import { closedModalWindow } from './closedModalWindow';
//
// export function eventClickWithoutModal(className, event) {
//   let targetNode = event.target;
//   while (!targetNode.classList.contains(className) && targetNode.localName !== 'body') {
//     targetNode = targetNode.parentNode;
//   }
//   if (targetNode.classList.contains(className)) {
//     return;
//   }
//   closedModalWindow(targetNode.id);
// }
